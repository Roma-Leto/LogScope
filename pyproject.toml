[project]
name = "logscope"
version = "0.1.0"
description = "LogScope — CLI-инструмент анализа HTTP-логов"
requires-python = ">3.11"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
